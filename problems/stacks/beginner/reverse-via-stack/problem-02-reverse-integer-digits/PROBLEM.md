# Reverse Digits of an Integer via Stack

**Difficulty:** Easy

**Source:** Classic — digit reversal

## Description

Given a non-negative integer `n`, reverse its decimal digits using a stack of digits and return the resulting integer (leading zeros in the reversed form are simply dropped by integer conversion).

## Hint

Push each digit; pop to assemble the reversed number.
