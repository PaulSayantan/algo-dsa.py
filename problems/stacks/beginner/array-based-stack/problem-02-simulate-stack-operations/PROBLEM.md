# Simulate Stack Operations

**Difficulty:** Easy

**Source:** Classic — op-sequence simulation

## Description

Given a list of operations, each either `["push", v]` or `["pop"]`, apply them to an initially empty stack and return the final stack contents from bottom to top.

## Hint

Append on push, list.pop() on pop; return the list at the end.
