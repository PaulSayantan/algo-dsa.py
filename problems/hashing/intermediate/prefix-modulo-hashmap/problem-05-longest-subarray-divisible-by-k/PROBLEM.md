# Longest Subarray Divisible by K

**Difficulty:** Medium

**Source:** Classic — longest subarray with sum divisible by k

## Description

Given an integer array `nums` and a positive integer `k`, return the length of the longest contiguous subarray whose sum is divisible by `k`.

## Hint

Earliest index of each prefix remainder; best = i - first[remainder].
